version: '3.1'

services:

  postgres:
    image: postgres:14.5
    restart: always
    environment:
      POSTGRES_PASSWORD: "password"
      POSTGRES_DB: rides_development
    volumes:
      - "./docker/.postgres:/var/lib/postgresql/data"
    ports:
      - "5432:5432"
